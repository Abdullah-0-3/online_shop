apiVersion: v1
kind: Service
metadata:
  name: online-shop
  namespace: online-shop
  labels:
    app: online-shop
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      nodePort: 30000
  selector:
    app: online-shop