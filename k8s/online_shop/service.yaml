apiVersion: v1
kind: Service
metadata:
  name: online-shop
  labels:
    app: online-shop
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
  selector:
    app: online-shop
